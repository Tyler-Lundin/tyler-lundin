import NavLinks from "./NavLinks"
import { _Nav } from "./styled"

const Nav = () => {
  return (
    <_Nav.Container>
      <_Nav.OpenButton>menu</_Nav.OpenButton>
    </_Nav.Container>
  )
}

export default Nav
